# Configuration de déploiement Netlify (site statique Astro).
[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"
  # Source de contenu = WordPress (headless), désormais sur le sous-domaine cms.
  PUBLIC_WP_API = "https://cms.hugobetelu.fr/wp-json"
